#include <stdio.h>
#include <stdbool.h>

int main() {
    bool finvotacion = false;
    char asignatura;
    int contP=0, contA=0, contM=0, contI=0, contC=0, totalvotos,maxvoto,minvoto;

    printf("Introduce la inicial de tu asignatura favorita: \n");
    scanf("%c",&asignatura);

   do{
       if
       //papeletas
       //leer voto
       //incrementar contP, cont M, contC,...
            switch (voto){
                cont 'P'= contP++;
                            break;
                default...;

            }
   }while (!finVOtacion);
   //mostrar resultados
   return 0;
}